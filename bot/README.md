# راهنمای استفاده از ربات سلف تلگرام 🤖

این یک ربات سلف (Userbot) قدرتمند و ماجولار است که با کتابخانه `Telethon` نوشته شده است.

## 🚀 نصب و راه اندازی

### ۱. دریافت API Key
ابتدا باید `API_ID` و `API_HASH` خود را از تلگرام دریافت کنید:
1. به سایت [my.telegram.org](https://my.telegram.org) بروید.
2. وارد حساب تلگرام خود شوید.
3. به بخش **API development tools** بروید.
4. یک برنامه جدید بسازید (نام و نام کوتاه دلخواه وارد کنید).
5. مقادیر `App api_id` و `App api_hash` را کپی کنید.

### ۲. تنظیمات
فایل `config.py` را باز کنید و مقادیر زیر را جایگزین کنید:
```python
API_ID = 123456          # جایگزین کنید با API_ID خودتان
API_HASH = "YOUR_HASH"   # جایگزین کنید با API_HASH خودتان
SESSION_NAME = "myselfbot" # نام فایل نشست (اختیاری)
PREFIX = "."             # پیشوند دستورات (مثلا .panel)
```

### ۳. اجرا با Docker 🐳

#### روش اول: استفاده از docker-compose (توصیه می‌شود)
```bash
# ساخت و اجرای کانتینر
docker-compose up -d

# مشاهده لاگ‌ها
docker-compose logs -f

# توقف ربات
docker-compose down
```

#### روش دوم: استفاده مستقیم از Docker
```bash
# ساخت ایمیج
docker build -t telegram-selfbot .

# اجرای کانتینر
docker run -it --name telegram-bot \
  -v $(pwd)/sessions:/app/sessions \
  -v $(pwd)/downloads:/app/downloads \
  telegram-selfbot

# توقف و حذف کانتینر
docker stop telegram-bot
docker rm telegram-bot
```

#### روش سوم: استفاده از اسکریپت `run.sh`
```bash
./run.sh
```

**نکته:** در اولین اجرا، شماره موبایل خود را وارد کنید و کد تایید ارسالی به تلگرام را بزنید. اطلاعات لاگین در پوشه `sessions` ذخیره می‌شود.

---

## 🛠 پلاگین‌ها و دستورات

### 📸 ذخیره ساز مدیا (Saver)
این پلاگین به صورت خودکار عکس‌ها و ویدیوهای تایم‌دار (Self-destruct) که برای شما در چت خصوصی ارسال می‌شود را دانلود کرده و در **Saved Messages** ذخیره می‌کند.

### 💬 پاسخ خودکار (AutoReply)
تنظیم پاسخ‌های آماده برای کلمات خاص. اگر کسی کلمه مورد نظر را بفرستد، ربات پاسخ می‌دهد.
- `.addreply کلمه پاسخ` : افزودن پاسخ جدید.
  - مثال: `.addreply سلام سلام خوبی؟`
- `.delreply کلمه` : حذف پاسخ برای یک کلمه.
- `.listreply` : لیست تمام پاسخ‌های خودکار.

### 🎙 اینوایت همگانی ویس (Inviter)
اینوایت کردن تمام اعضای یک گروه به ویس چت (Voice Chat).
- `.inviteall` : شروع اینوایت اعضای گروه به ویس چت فعال.
  - **نکته:** این دستور در گروه‌هایی که ویس چت فعال دارند کار می‌کند.

### 😄 ری‌اکشن خودکار (Reactor)
واکنش خودکار با اموجی به پیام‌هایی که حاوی کلمات خاص هستند.
- `.addreact کلمه اموجی` : افزودن واکنش جدید.
  - مثال: `.addreact جالب 🤔`
- `.delreact کلمه` : حذف واکنش.
- `.listreact` : لیست تمام واکنش‌های خودکار.

### 👤 ری‌اکشن به کاربر خاص (User Reactor)
واکنش خودکار به پیام‌های یک کاربر مشخص در یک گروه خاص.
- `.ur add اموجی` : تنظیم ریکشن برای کاربری که به پیامش reply زدی (فقط در همون گروه).
  - مثال: به پیام کسی reply بزن و بنویس `.ur add ❤️`
- `.ur del` : حذف ریکشن کاربری که به پیامش reply زدی.
- `.ur list` : مشاهده لیست ریکشن‌های تنظیم شده در این گروه.
- `.ur clear` : پاک کردن همه ریکشن‌ها در این گروه.
  - **نکته:** ریکشن‌ها مختص هر گروه هستند و در گروه‌های دیگر اعمال نمی‌شوند.


### ⚙️ پنل مدیریت (Manager)
- `.panel` : نمایش وضعیت ربات، لیست پلاگین‌های فعال و راهنمای دستورات.
- `.ping` : تست سرعت و آنلاین بودن ربات.

---

## ⚠️ نکته مهم
استفاده از سلف‌ربات‌ها (Userbots) ممکن است با سیاست‌های تلگرام در تضاد باشد. مسئولیت استفاده از این ربات با شماست. سعی کنید از دستورات رگباری (Spam) خودداری کنید تا اکانت شما محدود نشود.

---

## ☁️ استقرار در Cloud 66

برای استقرار سریع در Cloud 66 (Containers) روند پیشنهادی:

1. در Cloud 66 یک Stack جدید بسازید و نوع "Containers" یا استفاده از Dockerfile را انتخاب کنید.
2. در بخش Environment variables مقادیر زیر را اضافه کنید: `API_ID`, `API_HASH`, `BOT_TOKEN` (در صورت استفاده), `OWNER_ID`, `SESSION_NAME`.
3. برای حفظ نشست‌ها و دانلودها یک Persistent Volume بسازید و آن را به مسیرهای کانتینر `/app/sessions` و `/app/downloads` نگاشت کنید.
4. کانتینر را Deploy کنید. (CMD در `Dockerfile` به‌صورت `python main.py` است — نیازی به `Procfile` نیست مگر از Stack غیر-Container استفاده کنید.)
5. لاگ‌ها و وضعیت سرویس را در داشبورد Cloud 66 بررسی کنید.

نکات امنیتی:
- مقادیر حساس را در `config.py` ذخیره نکنید — از متغیرهای محیطی یا secret store استفاده کنید.
- اگر قبلا مقادیر حساس را در گیت پوش کرده‌اید، بلافاصله توکن‌ها/کلیدها را ری‌ویوک کنید.
